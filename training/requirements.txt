# Pinned to the verified reproducibility versions — matches
# training/verification_report.json; re-running main.py with these versions
# reproduces the exported artifacts.
pandas==3.0.5
numpy==2.4.6
scikit-learn==1.9.0
imbalanced-learn==0.14.2
skl2onnx==1.20.0
onnx==1.22.0
onnxruntime==1.29.0
